Note: App.jsx is different from .js because .jsx allows html to be written there.
Note: export is like public in c++ and default is like main in c++.
I got an error in the terminal and in the webpage instantly after copying and pasting the second button in App.jsx in the same row.
After using fragments the error disappeared and two boxes with X are displayed side by side in the upper left of the webpage. Adding seven more lines of code for the boxes of X, results in 9 of them in a row, not in the 3x3 expected square shape.
It seems the level of indentation doesn't matter as long as the element is correctly inside the fragment and simply serves to make the code more readable.
Each div is kind of like the new line character and puts the buttons on a row below.
Changing the name of the function from Square to Board didn't break anything and the styling is still the same because in style.css the square block is used as a className for the buttons.
Using the Square function where the button has a 1 and <Square /> in the Board function made all the squares have 1's.
The {value} prop is like a function parameter.
{}'s indicate a variable, without it, Javascript thinks it is a string or literal text.
Right after using the {value} prop, the board became empty, since in Board nothing has been passed to the <Square />'s, and I think the default of {value} when empty is the empty string.
Adding value="1", value="2", value="3", ... to the <Square />'s numbered them as they were before.
The order of the components corresponds to the line of code. Codewise top to bottom and board wise left to right and also top to bottom.
You can declare a function in a function. If a function needs to be used or called, it needs to be surrounded by {}'s too.
Note: onClick={handleClick} and not onClick={handleClick()}. onClick={handleClick()} will call handleClick even without clicking on the square. I saw this when I opened dev tools in Chrome browser and saw the console just load with a bunch of 'clicked!' but once changed, there would be this incrementing number next to one line of 'clicked!' in the console when I actually clicked any square.
After useState and adding setValue('X') to handleClick, an empty square will display X after being clicked and stay that way even if clicked again.
Note: unlike before where squares were literally numbered 1-9, after using the state Array, which index starts at 0, indexes 0-8 of the array were used.
There's an empty board again after creating the components in the Board function, and I think the default value at an array index that is empty is the empty string.
Note: when adding another parameter, add it with a comma after the previous parameter in the {}'s. You could also do funct({parameter1}, {parameter2}).
When handleClick only set index 0 of nextSquares to X and only the first <Square /> has onSquareClick set to handleClick, the top left square of the board would go from empty to displaying an X after it was clicked and stay that way even after subsequent clicks.
Trying to display using argument i in handleClick cause infinite reredure/nothing showing to the webpage, because as I noted earlier, {handleClick} vs {handleClick(0)}, where the latter will keep calling the function not depending on whether the square has been clicked. onSquareClick={() => handleClick(0)} fixed the issue. Adding onSquareClick={() => handleClick(#)} for all <Square />'s does the same for the rest of the board.
nextSquares is a modified copy of the original squares array because it preserves immutability in case previous game progress is important and gets used later. Additionally extra re-render is avoided.
Note: For consts like const [xIsNext, setXIsNext] = useState(true);, the first const in the list take the value of argument to useState and the second const is a function.
Adding the if statement for X's and O's to handleClick will alternate X and O between clicks and they override each other on the same square. The overwriting is avoided with the early return case.
By observation, lines in calculateWinner is all the indexes of columns, rows, and diagonals for which a player could win but filling up all with their character.
It doesn't matter the order in which functions are placed in the code.
After calculateWinner was added to the early return case in handleClick, if I got three in a row for either X or O the first time, then afterwards when I clicked, there would be no changes to the board.
Note: let variables can be reassigned values after definition/first declaration. const varible can still be changed but they'd be redefined (const var = smth; rather than var = smth;).
Note: initially I accidentally added the part for displaying the winner status to the handleClick function and when I added the div for status, the editor told me it was deprecated. Afterward, I moved that part of code outside handleClick but still inside Board and the warning was gone.
When I added the Game function and didn't remove export default from Board, I got an error, which is because there's two default functions, so removing default from Board was the solution, but I also removed export from Board, which means Board can't be called from outside the file, while Game can.
Where there used to be the winner status became the status for who the next player is.
Although there's quite a few div's in Game, it doesn't necessarily add more whitespace, when there's no extra information to be displayed.
Even when handlePlay is empty, the game was still functional because Game includes <Board ... /> and Board itself is functional.
After adding { xIsNext, squares, onPlay } as arguments to Board, removing the first two lines of const is required, because otherwise there'd be an error saying the variables had already been declared.
Note: in general whenever I'm in the middle of modifying my code and there's mismatched brackets or some other synax error, the error will show up instantly in the terminal and webpage.
The key error is because React stores some information about every list item, and needs to be able to identify each item.
There is such thing as strict equality operator that is === and loose equality is == which may yield different results for cases such as null and undefined. 